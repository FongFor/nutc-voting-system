# ============================================================
# NUTC Voting System — Caddy reverse proxy
#
# 只有這個 Caddy 容器對外開 80/443；CA/TPA/TA/CC/Admin 這些內部
# 服務不再對外發布 port（見 docker-compose.yml），只有選民端
# (voter) 與公告板 (BB) 透過這裡對外提供服務，全部走 HTTPS。
#
# v4.0 修正：Caddy → voter、Caddy → bb 這兩段以前是明文 HTTP（靠 Docker
# 網路隔離撐著），現在 voter/bb 兩個服務也各自監聽 TLS（見
# shared/tls_utils.py、voter_client/app.py、bb_server/app.py），upstream
# 改成 https://，並用 CA 根憑證驗證這兩個服務的伺服器憑證——這裡不是
# mTLS（Caddy 不持有這套 PKI 的用戶端憑證，也不需要，見 v4.0 規格書
# 第 26 章：voter/bb 的監聽埠本來就要接受一般瀏覽器的公開連線，不能要求
# 對方出示內部 PKI 的用戶端憑證）。CA 根憑證透過 docker-compose.yml 掛載
# 的 ca_keys 唯讀 volume 提供，見 /certs/ca_root_cert.pem。
#
# 本地測試（預設，不用改任何設定）：
#   docker compose up --build
#   瀏覽器開 https://localhost        → 選民端
#   瀏覽器開 https://bb.localhost     → 公告板
#   Caddy 會用內建的本地 CA 自動簽發憑證，第一次連線瀏覽器會顯示
#   「不受信任」警告，這是正常的（本地測試用的憑證不是公開 CA 簽的）。
#
# 正式上線（未來有網域之後）：
#   在 .env 設定：
#     SITE_ADDRESS=vote.your-domain.tld
#     BB_ADDRESS=bb.your-domain.tld
#   並確保該網域 DNS 已指向這台主機、80/443 對外可連通（Let's
#   Encrypt ACME 驗證需要），Caddy 會自動申請並持續更新正式憑證，
#   不需要再改這份 Caddyfile。
# ============================================================

{$SITE_ADDRESS:localhost} {
	# 存取紀錄：結構化 JSON，寫到 Caddy 自己的標準輸出，用
	# `docker compose logs caddy` 或 `docker compose logs -f caddy`
	# 即可看到每一筆連線的來源 IP、時間、方法、路徑、狀態碼。 <3
	log
	reverse_proxy https://voter:5005 {
		transport http {
			tls_trusted_ca_certs /certs/ca_root_cert.pem
		}
	}
}

{$BB_ADDRESS:bb.localhost} {
	log
	reverse_proxy https://bb:5004 {
		transport http {
			tls_trusted_ca_certs /certs/ca_root_cert.pem
		}
	}
}
